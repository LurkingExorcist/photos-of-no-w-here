import React from 'react';

import './App.css';
import { PhotoGrid } from '@/components';

const App: React.FC = () => {
    return (
        <div className="app">
            <PhotoGrid />
        </div>
    );
};

export default App;
